baseURL = 'https://www.gajabtools.com/'
languageCode = 'en-us'
title = 'GAJAB TOOLS'
theme = "my-tools"

[outputs]
  home = ["HTML", "JSON", "RSS"]
  page = ["HTML", "JSON"]

[outputFormats.channel]
mediaType = "application/json"
baseName = "channel"
isPlainText = true

[sitemap]
  changefreq = "daily"
  priority = 0.8
  filename = "sitemap.xml"

  [permalinks]
  posts = "/:slug/"